<p style="color: green"><%= notice %></p>

<div class="miningtype">
<%= render @mining_type %>

<div>
  <%= link_to t('links.edit'), edit_mining_type_path(@mining_type), class: "btn btn-success"%>
  <%= link_to t('links.back'), mining_types_path, class: "btn btn-secondary"%>
  <%= link_to t('links.delete'), @mining_type, method: :delete, class: "btn btn-danger" %>
</div>
</div>